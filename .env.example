# The public origin. Feeds metadataBase, canonicals, sitemap.xml, robots.txt.
NEXT_PUBLIC_SITE_URL=https://www.blueprintdigital.my

# Unlocks /proposals/*. Client documents carry another company's pricing, so
# middleware.ts fails closed: with this unset, every proposal link is locked.
# Never commit the real value; this repository is public.
PROPOSAL_PASSCODE=
